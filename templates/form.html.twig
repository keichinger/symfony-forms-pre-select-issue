{% extends "base.html.twig" %}
{% form_theme form "form_div_layout.html.twig" %}

{%- block title "Form" -%}

{%- block body -%}
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit">Submit</button>
    {{ form_end(form) }}
{%- endblock -%}
